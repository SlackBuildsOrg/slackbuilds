# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

          |-----handy-ruler------------------------------------------------------|
gitlab-cli: gitlab-cli (A GitLab CLI tool bringing GitLab to your command line)
gitlab-cli:
gitlab-cli: GLab is an open source GitLab CLI tool. It brings GitLab to your
gitlab-cli: terminal, next to where you are already working with git and your
gitlab-cli: code, without switching between windows and browser tabs.
gitlab-cli:
gitlab-cli: Homepage: https://gitlab.com/gitlab-org/cli
gitlab-cli:
gitlab-cli:
gitlab-cli:
gitlab-cli:
