wmuptime (dockapp that displays your uptime)

An LED display of your uptime, for WindowMaker and related window
managers.

Note: wmuptime by default runs in a normal (non-dockable) window. To
use it as a dockapp, run "wmuptime -w".
