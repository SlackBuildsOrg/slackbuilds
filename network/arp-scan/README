arp-scan (ARP scanning and fingerprinting tool)

Please read the man pages arp-scan(1), arp-fingerprint(1),
and get-oui(1) before using this tool.

Notes:

- This doesn't appear to work on wireless networks, only wired Ethernet.
- The binary is not installed setuid or with capability bits; root is
  required (or sudo).
