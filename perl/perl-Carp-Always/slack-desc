# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                |-----handy-ruler------------------------------------------------------|
perl-Carp-Always: perl-Carp-Always (Warns and dies noisily with stack backtraces)
perl-Carp-Always:
perl-Carp-Always: This module is meant as a debugging aid. It can be used to make
perl-Carp-Always: a script complain loudly with stack backtraces when warn()ing or
perl-Carp-Always: die()ing.
perl-Carp-Always:
perl-Carp-Always: HOMEPAGE: https://metacpan.org/release/Carp-Always
perl-Carp-Always:
perl-Carp-Always:
perl-Carp-Always:
perl-Carp-Always:
