XiMpLe is an xml and json editor based on a table grid view
(the same nodes are arranged in tables).

The editor is free for non-commercial use.

There are available also XML splitter, XML joiner for large
xml files and a light text editor.
