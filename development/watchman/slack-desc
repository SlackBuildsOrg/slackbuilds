# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

        |-----handy-ruler------------------------------------------------------|
watchman: watchman (file watching service)
watchman:
watchman: Watchman is a file watching service from Meta.
watchman: It watches files and records when they change.
watchman:
watchman: It can be used to trigger actions (such as rebuilding assets)
watchman: when matching files change.
watchman:
watchman: It is commonly used in React Native and JS tooling.
watchman:
watchman: Homepage: https://github.com/facebook/watchman
