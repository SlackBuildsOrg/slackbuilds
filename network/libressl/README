LibreSSL is a fork of OpenSSL developed by the OpenBSD project, focused
on modernizing the codebase and improving security.

Binaries are installed as: libressl-openssl, libressl-ocspcheck,
libressl-nc.

NOTE: This package includes libtls. Remove the standalone libtls package
before building and installing libressl.
