# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

           |-----handy-ruler------------------------------------------------------|
ngspice-jit: ngspice-jit (mixed-signal circuit simulator, ngspice fork)
ngspice-jit:
ngspice-jit: A fork of ngspice that JIT-compiles the behavioral R/C value
ngspice-jit: expressions foundry PDKs write (voltage/temperature-dependent
ngspice-jit: poly-resistors, MIM capacitors, high-field tanh laws) into native
ngspice-jit: OSDI devices at parse time, so the solver sees real charge- and
ngspice-jit: current-conserving devices instead of fragile behavioral sources.
ngspice-jit: It adds Eldo-style topology reduction, regex device/vector
ngspice-jit: introspection and a 'devices' command, and assorted fixes.
ngspice-jit:
ngspice-jit: Homepage: https://codeberg.org/alto555/ngspice
