# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

         |-----handy-ruler------------------------------------------------------|
openrsync: openrsync (BSD-licensed implementation of rsync)
openrsync:
openrsync: It's compatible with a modern rsync (3.1.3 is used for testing, but
openrsync: any supporting protocol 27 will do), but accepts only a subset of
openrsync: rsync's command-line arguments.
openrsync:
openrsync:
openrsync: Homepage: https://github.com/kristapsdz/openrsync
openrsync:
openrsync:
openrsync:
