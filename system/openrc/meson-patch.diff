diff --git a/init.d/meson.build b/init.d/meson.build
index 8ef77e3..be333c2 100644
--- a/init.d/meson.build
+++ b/init.d/meson.build
@@ -101,7 +101,7 @@ foreach init_d_file : init_data
     install_mode: 'rwxr-xr-x')
 endforeach
 install_symlink('functions.sh',
-  pointing_to: rc_shdir / 'functions.sh',
+  pointing_to: '../../../usr/libexec/rc/sh/functions.sh',
   install_dir: init_d_dir)
 
 if get_option('sysvinit') == true and os == 'Linux'
@@ -110,6 +110,6 @@ if get_option('sysvinit') == true and os == 'Linux'
     link_name = 'agetty.' + x
     install_symlink(link_name, install_dir: init_d_dir, pointing_to: 'agetty')
     install_symlink(link_name, install_dir: default_runlevel_dir,
-      pointing_to: init_d_dir / link_name)
+      pointing_to: '../../init.d/' + link_name)
   endforeach
 endif
diff --git a/meson.build b/meson.build
index d1c3ce1..3a3f6d2 100644
--- a/meson.build
+++ b/meson.build
@@ -210,11 +210,11 @@ util_dep = cc.find_library('util', required: false)
 subdir('bash-completion')
 subdir('conf.d')
 subdir('etc')
+subdir('sh')
 subdir('init.d')
 subdir('local.d')
 subdir('man')
 subdir('runlevels')
-subdir('sh')
 subdir('src')
 subdir('support')
 subdir('sysctl.d')
diff --git a/runlevels/meson.build b/runlevels/meson.build
index 56bc2f6..69f5a83 100644
--- a/runlevels/meson.build
+++ b/runlevels/meson.build
@@ -87,6 +87,6 @@ foreach runlevel: runlevels.keys()
   foreach service: runlevels[runlevel]
     install_symlink(service,
       install_dir: runlevel_dir / runlevel,
-      pointing_to: init_d_dir / service) 
+      pointing_to: '../../init.d/' + service)
   endforeach
 endforeach
