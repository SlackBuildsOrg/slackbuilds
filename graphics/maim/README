maim (make screenshots of desktop)

maim (make image) takes screenshots of your desktop. It has options to
take only a region, and relies on slop to query for regions.

maim is meant to to overcome shortcomings of scrot and performs better
in several ways.
