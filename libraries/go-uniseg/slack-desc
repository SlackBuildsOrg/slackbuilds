# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

         |-----handy-ruler------------------------------------------------------|
go-uniseg: go-uniseg (Go library implementing Unicode Text Segmentation)
go-uniseg:
go-uniseg: uniseg implements Unicode Text Segmentation (UAX #29) and Unicode
go-uniseg: Line Breaking (UAX #14): splitting strings into grapheme clusters,
go-uniseg: words/sentences, and finding valid line-break points, so multi-
go-uniseg: codepoint glyphs (emoji, combining marks) aren't miscounted.
go-uniseg:
go-uniseg: It's a source-only Go library (no binaries); go-runewidth
go-uniseg: depends on it for correct terminal-cell width measurement.
go-uniseg:
go-uniseg: Homepage: https://github.com/rivo/uniseg
