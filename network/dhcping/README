dhcping (checks if a remote DHCP server is alive)

dhcping sends a DHCP request to a DHCP server to see if it's up and
running. It allows the system administrator to check if a remote DHCP
server is still functioning.
